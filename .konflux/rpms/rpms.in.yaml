contentOrigin:
  repofiles:
    - ./ubi.repo
# Explicit openssl-libs avoids incomplete s390x lockfile entries after rpm lock regeneration.
packages: [openssh-clients, git, shadow-utils, openssl-libs]
arches:
  # The list of architectures
  - aarch64
  - x86_64
  - ppc64le
  - s390x
